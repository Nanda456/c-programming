#include <stdio.h>
void main()
{
    int i=1,fact=1,num;
    printf("enter a number\n");
    scanf("%d",&num);
    while(i<=num)
    {
        fact=fact*i;
        i++;
        
    }
    printf("factorial of %d is %d",num,fact);
}